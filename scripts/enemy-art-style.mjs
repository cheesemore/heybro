/** 全兵种共用：饥荒 / Klei 式手绘（与 gptimage 胸像批次一致，勿删语义） */
export const STYLE_WESTERN_INDIE_CARD = [
  `ART STYLE (fixed, applies to every generation): Klei "Don't Starve" inspired 2D illustration — hand-ink outlines with slightly jittery line weight,`,
  `flat or lightly cross-hatched fills, desaturated earthy palette, puppet-like stylized proportions, subtle grim whimsy.`,
  `NOT photoreal, NOT glossy anime cel, NOT soft airbrush beauty, NOT clean Pixar 3D.`,
  `Roughly three-head-tall readable silhouette; shadows as hatched or hard-edged ink blocks, not cinematic lighting.`,
  `Low saturation with grayed fantasy cast; clean readable game unit at small size.`,
].join(' ');
