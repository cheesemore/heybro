---
description: 战斗技能与单位解耦；技能默认可复用，非某首领专属
alwaysApply: true
---

# 战斗技能：与单位解耦

## 默认原则

- **技能不是某首领、英雄或怪物专属的**，除非设计文档或代码注释里**明确写明**「仅用于某某单位」。
- **技能逻辑独立**：实现按 `skillId`（`skills.json` + 战斗运行时处理）驱动，**不**按首领名、章节号、`bossUid`、单位实例绑死。
- **装配是数据层的事**：谁拥有哪些技能，只由配置里的 `skillIds`（如 `wowBookBosses.json`、英雄表、单位定义）决定；换单位挂上同一 `skillId` 就应走同一套逻辑。

## 命名说明

- `skillId` 可能带历史前缀（如 `skill_rhahk_smash`），**只表示技能类型 id**，**不表示**「只有拉克佐能用」。
- 新增技能时优先用**描述机制**的 id（如 `skill_boss_smash_windup`），避免在新代码里写「若是拉克佐才……」。

## skills.json 的 `params` 上限

- 每条技能 **`params` 最多 5 项**（`skillsCatalog.ts` 加载时校验，超出会抛错、游戏无法启动）。
- 新增技能时先规划 5 个槽位：通常 `[0]` 冷却、`[1]` 初冷却，其余留给伤害系数、范围/半径、触发阈值等**关键调参**。
- 第 6 个及以后的数值、表现时长、散布角度、额外眩晕秒数等 → **写死在战斗代码常量**（如 `BattleScreen` 里 `OVERLOAD_*`），并在该技能 `logicEffectCn` 注明「表外」。

## 实现时怎么做

- ✅ 在 `BattleScreen`（或共用战斗模块）里：`unitHasSkill(u, 'skill_xxx')` / `skillId === 'skill_xxx'` 分支处理。
- ✅ 状态字段可以跟技能走（如战吼叠层），任何**拥有该 skillId** 的单位都可复用。
- ✅ 测试页、书本首领战：只改 `bookChapterId` + 配置里的 `skillIds`，不要把技能写死在「某一关首领」分支里。
- ❌ 不要写 `if (bossName === '拉克佐')` 才放技能；不要默认「这是拉佐克专属技能」。
- ❌ 除非有**特别说明**，不要把技能 VFX/逻辑嵌进单一 Boss 类或单一立绘资源路径里而无法复用。

## 何时可以耦合

仅当产品/策划在需求或注释中**明确**需要单位专属行为时，才允许技能逻辑引用具体单位 id、章节或显示名；并在该处简短注明原因，方便以后拆回通用技能。
